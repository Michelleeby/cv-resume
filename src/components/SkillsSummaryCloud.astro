---
export interface Props {
  skills: string[];
}
const { skills } = Astro.props;
---

<!-- 
  We use 'flex-wrap' to create the "cloud" effect,
  and 'justify-center' to make it look balanced.
-->
<div class="flex flex-wrap gap-3 justify-center">
  {skills.map(skill => (
    <!-- 
      These tags are styled differently from the in-job tags:
      they are slightly larger and use the blue accent color
      to show they are a primary summary.
    -->
    <span class="bg-blue-100 text-blue-800 text-sm font-medium px-4 py-2 rounded-lg shadow-sm dark:bg-blue-900 dark:text-blue-200">
      {skill}
    </span>
  ))}
</div>